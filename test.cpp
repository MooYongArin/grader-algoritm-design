#include <bits/stdc++.h>

using namespace std;
struct State {
    int one;
    int two;

    bool operator<(const State &others) const {
        return one < others.one;
    }
};


int main() {
    int n, m;
}
