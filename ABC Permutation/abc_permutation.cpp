#include <iostream>
#include <vector>

using std::string, std::cin, std::cout, std::endl;

void my_recur(int n, int a, int b, int c){
    if (a != 0 && b != 0 &&  c != 0)
    {
        

    }
    
}
int main(){
    int n, i, j, k;
    // string str;
    my_recur(n,i,j,k);
    return 0;
}